---
argument_specs:
  main:
    options:
      my_list:
        type: list
        elements: dict
        default:
          - name: "Example"
        description: "A list of dicts."
        options:
          name:
            type: str
            required: true
            description: "Name of the item."
          value:
            type: int
            description: "Value of the item."
